This image is the browser-tab icon of an educational quiz game called Lahib, about the Saudi Highway Code.
It is FLAT VECTOR, not a photo or a render.

First look at assets/brand/lahib-mark.png if it exists, and reproduce the exact same road mark.

Generate the image now:
Size 512x512 px (perfect square), no transparency.
Subject: The same road mark (a tapering road ribbon in one-point perspective, bending gently to one side,
with three dashes up its centre line), rendered in solid white, centred on a solid #3C7162 rounded-square
background with a generous margin of about 18% on every side. Nothing else in the frame.
Flat, crisp, high contrast, legible when scaled down to 16x16 pixels.
Absolutely no text, no letters, no numerals.

Save the result as: assets/brand/lahib-icon.png
